# Web-Dev-Assignment-4
# Prompt Quizzer – JavaScript Console Quiz Game

Prompt Quizzer is a simple JavaScript-based quiz game that runs entirely in the browser console.  
The quiz asks predefined questions, takes user input using `prompt()`, provides instant feedback using `alert()`, and displays the final score at the end.

This project is created as part of **Web Development I – Lab Assignment 4**.

---

## 🚀 Features

- Runs completely in the browser console
- Uses JavaScript arrays and objects to store questions and answers
- Iterates through questions using a `for` loop
- Accepts user input using `prompt()`
- Provides immediate feedback using `alert()`
- Case-insensitive and space-trimmed answer validation
- Tracks and displays the final score

---

## 🛠 Technologies Used
- JavaScript (ES6)
- Browser Developer Console
  
## 📊 Output

- Displays feedback after each question (Correct / Wrong)
- Shows the correct answer if the user is wrong
- Displays the final score at the end of the quiz

---

## 📚 Learning Outcomes

- Understanding arrays and objects in JavaScript
- Using loops and conditional statements
- Handling user input and output
- Applying string methods like `toLowerCase()` and `trim()`
- Implementing basic game logic using JavaScript
